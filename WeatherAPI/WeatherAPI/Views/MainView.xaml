<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WeatherAPI.Views.MainView"
             Title="MainView">
    <VerticalStackLayout>
        
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            FontSize="Header"/>
        <SearchBar Placeholder="Inserir local" SearchCommand="{Binding SearchButtonPressedCommand}" Text="{Binding CityName}"/>
        <Label
            Text="{Binding CityName}"/>

        <Label Text="{Binding Temp}" />
        <!--<Label Text="{Binding Pressure}" />
        <Label Text="{Binding Humidity}" />
        <Label Text="{Binding WeatherId}" />
        <Label Text="{Binding Clime}" />
        <Label Text="{Binding ClimeDescription}" />
        <Label Text="{Binding ClimeIcon}" />
        <Label Text="{Binding Wind}" />
        <Label Text="{Binding Deg}" />
        <Label Text="{Binding Gust}" />-->

    </VerticalStackLayout>
</ContentPage>